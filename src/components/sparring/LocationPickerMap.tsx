import React from 'react';
import type { LocationPickerMapProps } from './LocationPickerMap.types';

// TypeScript type stub — Metro Bundler uses LocationPickerMap.ios.tsx / .android.tsx at runtime.
export default function LocationPickerMap(_props: LocationPickerMapProps): React.ReactElement {
  throw new Error('Platform-specific LocationPickerMap not found');
}
