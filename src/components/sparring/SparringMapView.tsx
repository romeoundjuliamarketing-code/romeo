import React from 'react';
import type { SparringMapViewProps } from './SparringMapView.types';

// TypeScript type stub — Metro Bundler uses SparringMapView.ios.tsx / .android.tsx at runtime.
export default function SparringMapView(_props: SparringMapViewProps): React.ReactElement {
  throw new Error('Platform-specific SparringMapView not found');
}
