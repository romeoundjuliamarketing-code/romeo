# Dev-Log – 2026-05-06

## Was wurde gebaut / geändert

### Fix 1 – PointsBreakdownScreen + Profil-Umbau
- `useFitnessRatings` + alte Punkte-Karte aus ProfilScreen entfernt
- Neuer Screen `PointsBreakdownScreen.tsx`: 13 Kategorien mit Donut-Diagramm (SVG, Blau-Palette #E8F4FD → #0A2B6B), Balken + XP-Liste
- Punkte-Karte im Profil jetzt tappbar (Chevron-Icon) → navigiert zu PointsBreakdown
- Navigation: `PointsBreakdownScreen` in `RootNavigator.tsx` und `types.ts` registriert

### Fix 2 – Kategorie-Mapping K1 (Groß-/Kleinschreibung)
- K1-Punkte wurden als "K1" gespeichert, Code suchte nach "k1" → 0 angezeigt
- Mapping vereinheitlicht, Groß-/Kleinschreibung normalisiert
- Alte Einträge "Boxen" und "Kraft & Ausdauer" (freie Texteingabe) bleiben in DB, erscheinen nicht in PointsBreakdown (korrekt so)
- Alte Mobility-Logs mit `training_type: null` können nicht mehr zugeordnet werden

### Fix 3 – Mobilität, Ausdauer, Partnertraining
- `useDailyStretch.ts`: `training_type: 'mobilitaet'` ergänzt
- `useDailyMobility.ts`: `training_type: 'mobilitaet'` ergänzt
- `ExtraTab.tsx`: `training_type: 'ausdauer'` für Joggen/Schwimmen/Seilspringen/Radfahren
- `WorkoutStartSheet.tsx`: Partnertraining entfernt, Nacken & Hals / Griffkraft hinzugefügt; `training_type` nutzt korrekten DB-Key statt `result.intensity`

### Sonstiges
- Dennis Fatiyan (dennis.fatiyan@web.de) manuell Individual-Abo bis 2099 in Supabase eingetragen

## Warum
- Punkte-System soll nach Disziplin/Kategorie differenziert anzeigen
- Trainingsverteilung im Profil aussagekräftiger machen
- Bugfixes für fehlende Kategorie-Zuweisung bei Mobility, Ausdauer, Stretch

## Offene Probleme
- Alte März-Einträge (490/350/385 XP) ohne Kategorie — können nicht zugeordnet werden
- Obsidian MCP noch nicht verbunden (API-Key prüfen)
- iPad Screenshots sauber nacharbeiten (aktuell flüchtig)

## Nächste Schritte
- v1.0.2 bei Apple einreichen (Onboarding Keyboard-Fix + Stundenplan RLS-Fix)
- Marketing / TikTok-Content starten
- Obsidian MCP-Verbindung reparieren
